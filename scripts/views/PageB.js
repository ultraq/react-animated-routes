
import classNames from 'classnames';
import React from 'react';

const PageB = ({className}) => (
	<section className={classNames('page', 'page-b', className)}>
		<header>
			<h1>Page B</h1>
		</header>
	</section>
);

export default PageB;
