
import classNames from 'classnames';
import React from 'react';

const PageC = ({className}) => (
	<section className={classNames('page', 'page-c', className)}>
		<header>
			<h1>Page C</h1>
		</header>
	</section>
);

export default PageC;
